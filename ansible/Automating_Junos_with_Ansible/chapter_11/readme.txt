Password for the sample vault files: Passw0rd

The first part of this chapter uses file group_vars/all.yaml,
while the latter part of this chapter uses directory group_vars/all/.
Only one of these should exist, so rename the other. The Git repository
has file group_vars/all.yaml correctly named, and the directory
renamed to group_vars/_all_/.
